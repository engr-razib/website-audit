{
  "name": "website-audit-by-razib",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.8.2",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "node clean.js && turbo run dev",
    "build": "turbo run build",
    "build:cpanel": "node build-cpanel.js",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "clean": "node clean.js"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
